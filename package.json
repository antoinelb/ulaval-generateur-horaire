{
    "name": "ulaval-generateur-horaire-e2e",
    "private": true,
    "type": "module",
    "description": "Tests de bout en bout de l'interface (make e2e). Le produit est en Rust : ce paquet n'existe que pour Playwright.",
    "scripts": {
        "test": "playwright test"
    },
    "devDependencies": {
        "@playwright/test": "^1.55.0"
    }
}
