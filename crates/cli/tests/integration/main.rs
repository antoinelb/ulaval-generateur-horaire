// Entry point for the `integration` test binary; the submodule drives the
// compiled binary end to end. A subdirectory with a `main.rs` is one test
// target, so the e2e tests share a single compilation.
mod cli;
