#![cfg_attr(coverage_nightly, feature(coverage_attribute))]

// dead_code: allowed until the scraper binary actually drives the module
#[allow(dead_code)]
mod print;

fn main() {
    println!("Scraper: not implemented yet");
}
